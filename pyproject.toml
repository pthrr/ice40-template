[project]
name = "ice40-template"
version = "0.1.0"
description = "Amaranth template for iCE40 FPGA with SystemVerilog integration"
requires-python = ">=3.9"
dependencies = [
    "amaranth>=0.5",
    "amaranth-boards @ git+https://github.com/amaranth-lang/amaranth-boards.git",
    "nox>=2024.4.15",
]

[dependency-groups]
dev = [
    "pytest>=8.3.3",
    "pytest-cov>=5.0.0",
    "ty>=0.0.1a31",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.metadata]
allow-direct-references = true

[project.entry-points."amaranth.sv_sources"]
ice40_template = "ice40_template:sv_sources"

[tool.hatch.build.targets.wheel]
packages = ["ice40_template"]

[tool.hatch.build.targets.wheel.force-include]
"rtl" = "ice40_template/rtl"

[tool.ruff]
line-length = 120
indent-width = 4

[tool.ruff.lint]
select = ["E", "F", "B", "I", "Q", "SIM", "C90", "TID", "PLC", "PIE", "B9"]
ignore = ["E203", "E501"]
fixable = ["ALL"]
unfixable = []

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
line-ending = "auto"

[tool.pytest.ini_options]
minversion = "8.0"
addopts = "-ra -q --strict-markers"
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]

[tool.coverage.run]
source = ["ice40_template"]
omit = ["tests/*", "**/__pycache__/*"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise AssertionError",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
]
